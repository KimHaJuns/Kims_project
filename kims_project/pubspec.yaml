name: kims_project
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.8.0

dependencies:
  flutter:
    sdk: flutter
  intl: ^0.18.1  # ← 여기 콜론(:) 빠진 거 고쳤어요!
  file_picker: ^6.1.1
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  # 예시 이미지 경로 설정 (원할 경우 추가)
  # assets:
  #   - assets/images/
